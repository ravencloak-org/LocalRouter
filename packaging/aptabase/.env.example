# Set these in Dokploy's Environment panel for the "raven / aptabase" compose service.
APTABASE_BASE_URL=https://aptabase.raven.yourdomain.com
APTABASE_AUTH_SECRET=  # openssl rand -hex 32
APTABASE_PG_PW=        # openssl rand -hex 16
APTABASE_CH_PW=        # openssl rand -hex 16
